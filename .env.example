# ── NetDad Environment ─────────────────────────────
# This file is gitignored (local dev only).
# On Vercel → set these in Project → Settings → Environment Variables.

# Canonical public URL (used for sitemap, OG tags, robots)
NEXT_PUBLIC_SITE_URL=https://netdad.ir

# Analytics (Plausible — optional, add after deploy)
NEXT_PUBLIC_ANALYTICS_DOMAIN=